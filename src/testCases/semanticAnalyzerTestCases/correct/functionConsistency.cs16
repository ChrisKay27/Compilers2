-- Chris Kaebe, March 3 2016


void foo(int x, ref bool b, int ab, ref bool bar){

    if( x = ab ) b := true;
    else bar := true

}

void main(void){
    bool b, bar;
    int x;
    x := 2;
    int a[5];
    int b;
    b := 2;

    foo(x+x, not y, (a[b]), bar);
    writebool(b);
    writebool(bar);

}