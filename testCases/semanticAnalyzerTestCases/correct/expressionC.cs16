-- Chris Kaebe, March 3, 2016

int main(void){
    int x, a;--, b, p, q;

    x := -a;
    x := x + 1;
   -- x := b;
   -- x := p < q;
   -- x := p <= q;
   -- x := p > q;
   -- x := p >= q;
   -- x := p = q;
   -- x := p /= q;

   return 0;
}