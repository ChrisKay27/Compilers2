-- Chris Kaebe, March 24, 2016

int one(void){
    return 1;
}

int add2(int x, int y){
    return x + y;
}


int main(void){
   -- writeint(1);
    writeint(one());
    --writeint(one()+one());
    writeint(add2(one(),one()));
    --int x;
    --x := 1;
    --writeint(x);
    return 1;--x;
}